# リッチメニュー運用手順（今すぐ版 + 後で拡張版）

二度手間を避けるため、まずは「Messaging APIなしで今すぐ運用」を先に進め、  
権限が整ったらそのまま「Messaging API版」に切り替える前提でまとめています。

## 今すぐ使う確定値

- `公式ホームページURL`  
  `https://kagayakuyakuzaisi.co.jp/`
- `FORM_BASE_URL`  
  `https://docs.google.com/forms/d/e/1FAIpQLSe0OA5PTUxue1euzhCR3SxkYtZdv7QxNKrTzoRW9TSQTRFkLg/viewform`
- `ENTRY_KEY`  
  `entry.1840678859`
- `LIFF_ID`  
  未確定（Messaging API/LINE Developersが使えるようになったら設定）

## A. 今すぐ進める（Messaging APIなし）

### A-1. LINE公式アカウントマネージャーでリッチメニューを作る

画像: `S__418275385.jpg`

推奨アクション:
- 求人情報: URL（求人一覧ページ）
- 公式ホームページ: URL（公式HP）
- 担当者連絡先: URL（担当者一覧または問い合わせフォーム）
- 使い方: テキスト送信（`使い方を知りたい`）

※ 上段タブ（薬剤師/企業様）の切替は、現時点では未対応でOK。

### A-2. キーワード応答を設定する

最低限、以下を設定:
- `使い方を知りたい`
- `求人情報を見たい`（運用する場合）
- `担当者一覧を見たい`（運用する場合）

文面は `キーワード応答テンプレ_薬剤師.txt` を利用。

### A-3. URL運用を先に固定する

後でAPI化しても同じURLを使えるよう、先に公開URLを決めておく:
- 求人一覧ページURL
- 公式HP URL
- 担当者一覧 / 問い合わせURL

## B. 後で切り替える（Messaging API利用可になったら）

### B-1. LIFF設定（フォーム自動入力）

1. LINE Developersで対象チャネルを開く
2. LIFFを追加（Size: Full / Scope: `profile`）
3. `LIFF ID` を取得

### B-2. `index.html` を設定して公開

`index.html` の `CONFIG` を更新:
- `LIFF_ID`
- `FORM_BASE_URL`
- `ENTRY_KEY`

公開後、リッチメニューのフォーム導線URLを公開URLへ変更。

### B-3. API版リッチメニューへ移行

`create_pharmacist_richmenu.py` を使って、上段タブ付きメニューへ切替。  
企業側メニュー未作成の場合は、企業タップ時に準備中メッセージを返せる。

### B-4. 解放運用（必要時）

`unlock_richmenu.py` で回答者へのメニュー切替を自動化可能。

### B-5. 企業「お問い合わせ」でクイックリプライ3択を出す

対象ファイル: `company_webhook.py`

1. Railway などに `company_webhook.py` をデプロイ
2. 環境変数を設定
   - `LINE_CHANNEL_ACCESS_TOKEN`
   - `LINE_CHANNEL_SECRET`
3. LINE Developers の Webhook URL を `https://<your-domain>/webhook` に設定
4. Webhook を有効化
5. 企業リッチメニューの右ボタンは `問い合わせしたい` を送信する設定にする

これで、ユーザーが `お問い合わせ` を押すと以下3択のクイックリプライが表示される:
- フリーランス薬剤師の募集
- 転職の仲介
- その他のお問い合わせ
