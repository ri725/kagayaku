# ミーティングで依頼すること（最小）

## 目的

Googleフォーム送信者だけ、薬剤師向けリッチメニューを解放する。

## 依頼事項（この3つだけ）

1. 既存Messaging APIチャネルでLIFFを1つ作成
   - Name: `form-redirect`
   - Size: `Full`
   - Scope: `profile` をON
   - Endpoint URL: 仮で `https://example.com` で可
2. `LIFF ID` を共有
3. `Channel access token`（long-lived）を発行して共有

## 共有してほしい情報

- `LIFF ID`
- `Channel access token`
- （任意）`Channel ID` の再共有

## 依頼文（コピペ）

既存Messaging APIチャネルで以下の対応をお願いします。  
1) LIFFアプリ作成（name: form-redirect / size: Full / scope: profile ON / endpointは仮URLで可）  
2) LIFF IDの共有  
3) Channel access token（long-lived）の発行と共有  
目的は、Googleフォーム送信者だけリッチメニューを自動解放するためです。
